{% comment %} Collection hero banner {% endcomment %}
<section class="collectionHero section">
    <h2 class="collectionHero__heading">{{collection.title}}</h2>
    <img src="{{ collection.featured_image | img_url : 'master' }}" class="collectionHero__image">
</section>