{% comment %} Collection hero banner {% endcomment %}
<section class="collectionHero section">
  <h1 class="collectionHero__title">{{ collection.title }}</h1>
  <img class="collectionHero__image" src="{{ collection.image | img_url: 'master' }}" alt="{{ collection.image.alt }}">
</section>
